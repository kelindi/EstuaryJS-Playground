import * as React from 'react';
import {Link } from 'react-router-dom';
type AppProps = {
}; 


const Navigation = (props: AppProps) => {
  return <React.Fragment>
    {/* <Link to="/putFile">Put File</Link>
    <Link to="/getPins">Get Pins</Link> */}
    
  </React.Fragment>;
};

export default Navigation;
